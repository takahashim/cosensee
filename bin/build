#!/usr/bin/env ruby
# frozen_string_literal: true

require 'bundler/setup'
require 'cosensee'

project = Cosensee::Project.parse_file('togetogebox.json')
builder = Cosensee::HtmlBuilder.new(project)
builder.build_all
